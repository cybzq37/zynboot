server:
  port: 28000

spring:
  application:
    name: zyn-gateway
  profiles:
    active: @spring.profiles.active@
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379
      password: ${REDIS_PASSWORD:Zyn@secure#99}
      database: 0
  cloud:
    gateway:
      routes:
        - id: system
          uri: ${SYS_BASE_URL:http://localhost:28081}
          predicates:
            - Path=/sys/**
          filters:
            - StripPrefix=1
        - id: demo
          uri: ${DEMO_BASE_URL:http://localhost:28080}
          predicates:
            - Path=/demo/**
          filters:
            - StripPrefix=1
      globalcors:
        cors-configurations:
          '[/**]':
            allowed-origins: "*"
            allowed-methods: "*"
            allowed-headers: "*"
            max-age: 3600

sa-token:
  token-name: Authorization
  timeout: 2592000
  active-timeout: -1
  is-concurrent: true
  is-share: false
  token-style: uuid
  is-log: true

logging:
  level:
    com.zynboot: debug
    org.springframework.cloud.gateway: debug
